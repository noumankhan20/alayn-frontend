import PosTerminalComponent from "@/components/pos/PosTerminalComponent";

export const metadata = {
  title: "POS Terminal | Alayn POS",
  description: "Interactive point of sale terminal for counter checkout and order creation.",
};

export default function PosPage() {
  return <PosTerminalComponent />;
}
